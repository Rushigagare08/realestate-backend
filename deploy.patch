diff --git a/deploy-ready.patch b/deploy-ready.patch
new file mode 100644
index 0000000000000000000000000000000000000000..40f116b11736bed9aa29cf35fc45d0b390a23ede
--- /dev/null
+++ b/deploy-ready.patch
@@ -0,0 +1,230 @@
+From 40d91ad20d0538ca2231b5f3f9db06b806f046e0 Mon Sep 17 00:00:00 2001
+From: Codex <codex@openai.com>
+Date: Mon, 16 Feb 2026 12:41:43 +0000
+Subject: [PATCH] Codex-generated pull request
+
+Codex generated this pull request, but encountered an unexpected error after generation. This is a placeholder PR message.
+---
+ .env.example                  |  5 +++
+ README.md                     | 58 +++++++++++++++++++++++++++++++++++
+ backend/index.js              | 51 ++++++++++++++++++------------
+ frontend/src/Pages/SignIn.jsx |  2 +-
+ frontend/src/Pages/SignUp.jsx |  2 +-
+ package.json                  |  1 +
+ 6 files changed, 98 insertions(+), 21 deletions(-)
+ create mode 100644 .env.example
+ create mode 100644 README.md
+
+diff --git a/.env.example b/.env.example
+new file mode 100644
+index 0000000..038967b
+--- /dev/null
++++ b/.env.example
+@@ -0,0 +1,5 @@
++MONGODB_URL=mongodb+srv://<user>:<password>@<cluster>/<db>
++JWT_SECRET=replace-with-a-strong-secret
++CLIENT_URL=http://localhost:5173
++PORT=3000
++NODE_ENV=development
+diff --git a/README.md b/README.md
+new file mode 100644
+index 0000000..a25275b
+--- /dev/null
++++ b/README.md
+@@ -0,0 +1,58 @@
++# Real Estate Backend + Frontend Deployment Guide
++
++This project is now configured for **single-service deployment** (Express serves the built Vite app in production).
++
++## 1) Required environment variables
++
++Create these variables in your hosting provider:
++
++- `MONGODB_URL` - MongoDB connection string
++- `JWT_SECRET` - JWT signing secret used by auth controllers
++- `CLIENT_URL` - Public frontend URL (used for CORS), for example `https://your-app.onrender.com`
++- `NODE_ENV` - set to `production`
++
++For local development, keep using your local `.env` file.
++
++## 2) Deploy on Render (recommended for this setup)
++
++1. Push this repository to GitHub.
++2. In Render, create a **Web Service** connected to this repo.
++3. Configure:
++   - **Build Command:** `npm install && npm run build`
++   - **Start Command:** `npm start`
++4. Add the environment variables above.
++5. Deploy.
++
++Render will:
++- install backend dependencies,
++- build the frontend into `frontend/dist`,
++- run `node backend/index.js`,
++- and Express will serve both API (`/api/*`) and frontend routes.
++
++## 3) Local production smoke test (optional)
++
++```bash
++npm install
++npm run build
++NODE_ENV=production npm start
++```
++
++Open `http://localhost:3000` for the frontend and `http://localhost:3000/api/health` for backend health.
++
++## 4) Development workflow
++
++Run backend dev server from repo root:
++
++```bash
++npm run dev
++```
++
++Run frontend separately in another terminal:
++
++```bash
++cd frontend
++npm install
++npm run dev
++```
++
++Frontend dev server proxies `/api` to `http://localhost:3000` via Vite config.
+diff --git a/backend/index.js b/backend/index.js
+index b0e01fa..5edcf7d 100644
+--- a/backend/index.js
++++ b/backend/index.js
+@@ -1,55 +1,69 @@
+-// backend/index.js  (replace your current file content or patch accordingly)
+ import express from "express";
+ import dotenv from "dotenv";
+ import mongoose from "mongoose";
+ import cookieParser from "cookie-parser";
+ import cors from "cors";
++import path from "path";
++import { fileURLToPath } from "url";
+ import userRouter from "./routes/user.route.js";
+ import authRouter from "./routes/auth.route.js";
+ import listingRouter from "./routes/listing.route.js";
+ import adminRouter from "./routes/admin.route.js";
+ 
+-// import listingRoutes from "./routes/listing.route.js"; // <--- add this
+-
+ dotenv.config();
+ const app = express();
+ 
++const __filename = fileURLToPath(import.meta.url);
++const __dirname = path.dirname(__filename);
++
+ app.use(express.json());
+ app.use(cookieParser());
+ 
+-// enable CORS for your frontend origin and allow cookies
++const allowedOrigins = [
++  "http://localhost:5173",
++  "http://localhost:5174",
++  ...(process.env.CLIENT_URL ? [process.env.CLIENT_URL] : []),
++];
++
+ app.use(
+   cors({
+-    origin: [
+-      "http://localhost:5173",
+-      "http://localhost:5174",
+-      "https://your-frontend.vercel.app",
+-    ],
++    origin: (origin, callback) => {
++      if (!origin || allowedOrigins.includes(origin)) {
++        return callback(null, true);
++      }
++      return callback(new Error("Not allowed by CORS"));
++    },
+     credentials: true,
+   })
+ );
+ 
+-
+-
+-// MongoDB connect
+ mongoose
+   .connect(process.env.MONGODB_URL)
+   .then(() => console.log("Connected to MongoDB"))
+   .catch((err) => console.error("Could not connect to MongoDB", err));
+ 
+-// test
+-app.get("/", (req, res) => {
+-  res.send("Backend is working!");
++app.get("/api/health", (req, res) => {
++  res.status(200).json({ status: "ok" });
+ });
+ 
+-// register routes
+ app.use("/api/user", userRouter);
+ app.use("/api/auth", authRouter);
+-app.use("/api/listing", listingRouter); // <--- mount listing routes
++app.use("/api/listing", listingRouter);
+ app.use("/api/admin", adminRouter);
+ 
++if (process.env.NODE_ENV === "production") {
++  const frontendDistPath = path.resolve(__dirname, "../frontend/dist");
++  app.use(express.static(frontendDistPath));
++
++  app.get(/^(?!\/api).*/, (req, res) => {
++    res.sendFile(path.join(frontendDistPath, "index.html"));
++  });
++} else {
++  app.get("/", (req, res) => {
++    res.send("Backend is working!");
++  });
++}
+ 
+-// global error handler (keeps as-is)
+ app.use((err, req, res, next) => {
+   const statusCode = err.statusCode || 500;
+   const message = err.message || "Internal Server Error";
+@@ -59,4 +73,3 @@ app.use((err, req, res, next) => {
+ app.listen(process.env.PORT || 3000, () => {
+   console.log("Server running");
+ });
+-
+diff --git a/frontend/src/Pages/SignIn.jsx b/frontend/src/Pages/SignIn.jsx
+index 8954f7c..c7f4606 100644
+--- a/frontend/src/Pages/SignIn.jsx
++++ b/frontend/src/Pages/SignIn.jsx
+@@ -4,7 +4,7 @@ import { useDispatch } from "react-redux";
+ import { signInStart, signInSuccess, signInFailure } from "../redux/user/userSlice";
+ import { useNavigate, Link } from "react-router-dom";
+ import { toast } from "react-toastify";
+-import Oauth from "../components/Oauth";
++import Oauth from "../components/OAuth";
+ 
+ export default function SignIn() {
+   const dispatch = useDispatch();
+diff --git a/frontend/src/Pages/SignUp.jsx b/frontend/src/Pages/SignUp.jsx
+index dddcc22..1e8bf13 100644
+--- a/frontend/src/Pages/SignUp.jsx
++++ b/frontend/src/Pages/SignUp.jsx
+@@ -2,7 +2,7 @@
+ import { useState } from "react";
+ import { Link, useNavigate } from "react-router-dom";
+ import { toast } from "react-toastify";
+-import Oauth from "../components/Oauth";
++import Oauth from "../components/OAuth";
+ 
+ export default function SignUp() {
+   const navigate = useNavigate();
+diff --git a/package.json b/package.json
+index 9183217..a60316c 100644
+--- a/package.json
++++ b/package.json
+@@ -6,6 +6,7 @@
+   "main": "index.js",
+   "scripts": {
+     "dev": "nodemon backend/index.js",
++    "build": "npm ci --prefix frontend && npm run build --prefix frontend",
+     "start": "node backend/index.js"
+   },
+   "dependencies": {
+-- 
+2.43.0
+
